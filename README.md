# Java Basics / Java Temelleri

This repository serves as a comprehensive "Cheat Sheet" for Java fundamentals. It covers everything from basic operators to control flow structures.<br>
Bu depo, Java temelleri için kapsamlı bir "özet kılavuz" niteliğindedir. Temel operatörlerden kontrol akış yapılarına kadar her şeyi kapsar.

---

## 🇺🇸 English - Technical Overview

### 📚 Content
- **Operators:** Detailed list of Arithmetic, Comparison, and Assignment operators.
- **String Manipulation:** Methods like `.length()`, `.toUpperCase()`, and `.indexOf()`.
- **Math Class:** Usage of `Math.max()`, `Math.min()`, `Math.sqrt()`, and random number generation.
- **Control Flow:** Implementation of `if-else` and `switch-case` logic.
- **Loops:** Examples of `for`, `for-each`, and `while` loop structures.

---

## 🇹🇷 Türkçe - Teknik Özet

### 📚 İçerik
- **Operatörler:** Aritmetik, Karşılaştırma ve Atama operatörlerinin detaylı listesi.
- **String İşlemleri:** `.length()`, `.toUpperCase()` ve `.indexOf()` gibi temel metodlar.
- **Math Sınıfı:** `Math.max()`, `Math.min()`, `Math.sqrt()` kullanımı ve rastgele sayı üretimi.
- **Kontrol Akışı:** `if-else` ve `switch-case` mantığının uygulanışı.
- **Döngüler:** `for`, `for-each` ve `while` döngü yapılarına dair örnekler.
